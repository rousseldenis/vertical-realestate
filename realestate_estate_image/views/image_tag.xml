<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2020 ACSONE SA/NV
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
  <record model="ir.ui.view" id="image_tag_form_view">
    <field name="name">image.tag.form (in realestate_estate_image)</field>
    <field name="model">image.tag</field>
    <field name="inherit_id" ref="TODO othermodule.form_view" />
    <field name="arch" type="xml">
            <!-- TODO -->
        </field>
  </record>
  <record model="ir.ui.view" id="image_tag_search_view">
    <field name="name">image.tag.search (in realestate_estate_image)
        </field>
    <field name="model">image.tag</field>
    <field name="inherit_id" ref="TODO othermodule.search_view" />
    <field name="arch" type="xml">
            <!-- TODO -->
        </field>
  </record>
  <record model="ir.ui.view" id="image_tag_tree_view">
    <field name="name">image.tag.tree (in realestate_estate_image)</field>
    <field name="model">image.tag</field>
    <field name="inherit_id" ref="TODO othermodule.tree_view" />
    <field name="arch" type="xml">
            <!-- TODO -->
        </field>
  </record>
  <record model="ir.actions.act_window" id="image_tag_act_window">
    <field name="name">Image Tag</field>
    <!-- TODO -->
    <field name="res_model">image.tag</field>
    <field name="view_mode">tree,form</field>
    <field name="domain">[]</field>
    <field name="context">{}</field>
  </record>
  <record model="ir.ui.menu" id="image_tag_menu">
    <field name="name">Image Tag</field>
    <field name="parent_id" ref="xmlid of parent menu" />
    <!-- TODO -->
    <field name="action" ref="image_tag_act_window" />
    <field name="sequence" eval="16" />
    <!-- TODO -->
  </record>
</odoo>
